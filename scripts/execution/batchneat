#!/bin/bash

if [ $# != 2 ] ; then
    echo "usage: "$0" PREFIX NAME"
    exit
fi

PREFIX="${1}"
NAME="${2}"
NEDIR=~/java/ne
GPATDEMOCFG=${NEDIR}/cfg/demo/neatdemo.properties


function run {
    sed -i "" "1s/.*/INCLUDE = "${3}"\/"${4}"\.properties/" ${GPATDEMOCFG}
    ./nepackage ${1} ${PREFIX}"_"${4}_${NAME}
    ./runssh ${2}
}

run "NEAT" "kovarik:~/nepack1" "regression" "1D-1"
run "NEAT" "drchal:~/nepack2" "regression" "2D-1"
run "NEAT" "drchal:~/nepack3" "regression" "3D-1"
run "NEAT" "drchal:~/nepack4" "regression" "4D-1a"
run "NEAT" "kovarik:~/nepack5" "regression" "4D-1b"
run "NEAT" "drchal:~/nepack6" "maze" "maze-1"
run "NEAT" "drchal:~/nepack7" "maze" "maze-2"
